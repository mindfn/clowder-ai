---
feature_ids: [F117]
topics: [message-delivery, a2a, roadmap, fork, upstream]
doc_kind: implementation-plan
created: 2026-09-24
---

# F117 / #1398 推进路线（2026-09-24 起）

> 维护：布偶猫 opus（实现）· 狸花猫 kimi（交叉审查）· 布偶猫 Fable（大的架构取舍）。09-25 起按 Landy 调整的分工（见约定 10）
>
> 每合入一个 PR，就更新本文的进度表。这条工作线的进度以本文为准，不以聊天记录为准。

## 为什么有这份路线

2026-09-24 起，吴浪出行约半个月，由 clowder-ai maintainer Landy 代为照看。Landy 和 opus 在
thread `thread_msr51149hym0i79f` 对齐后（消息 `…1279`、`…1294`、`…1299`、`…1304`），改用下面的推进方式。

## 约定

1. **按 phase 推进，一个 phase 一个 PR。** 一个 phase 可以有多个 commit，但只提一个 fork PR，合进
   develop_base（也就是 3001 运行的版本）。不要拆得太碎。
2. **人工体验在 3001 上做。** Landy 调整了人工体验的方式，不再做 worktree 隔离体验。开发和自动化验证仍然在
   隔离环境里做（独立 worktree、独立 Redis）。合入 develop_base 之前，相关测试要通过，运行实例自己的构建也要
   通过（shared、MCP、API 的构建和前端 `next build`，与 3001 重启时执行的一致），交叉审查要通过。
3. **合入只代表进入真实体验，不代表体验通过。** 进度表里每个 phase 都要记录：
   - 状态：待合入 / 已合入待加载 / 体验中 / 已确认；
   - PR、合入 SHA，以及 3001 实际在跑的 SHA，避免把新代码和还没重启的旧进程混为一谈。
4. **回退边界。** 合入前的 develop_base SHA 是**代码**回退点，不代表数据也能跟着回退。涉及持久数据格式或写入
   语义变化的 phase，合入前要确认旧版本能读取新写入的数据；回退不能靠清空或倒退用户数据。
5. **开工前讲清范围、验收标准和技术依据。** 已有的设计直接引用，不再重写一遍。
6. **审查盯正确性和用户能看到的行为，不做防御性加码。** 同一个 phase 审到第 3 轮还没收敛：技术分歧由 opus 和
   审查者先收敛，大的架构取舍拉上 Fable；只有产品取舍、或需要改变已有技术决定时，才交给 Landy。
7. **每个 PR 合入后，更新本文的进度表。**
8. **决策分工。** 技术决策以吴浪（clowder-ai 架构师）已有的决定为准，见 F117 文档的 Key Decisions；产品、
   愿景和体验由 Landy 给反馈。需要拍板时直接 @ Landy。
9. **上游不拆 PR。** fork 这边体验没问题后，上游发一个版本，锁定基线并冻结合入；#1398 rebase 到最新，启动一个
   **包含 rebase 后全部改动的开源候选版本**来体验；确认没问题，再以一个 PR 合入。maintainer 09-21 提出的
   验收条件（同一个 SHA 连续 soak 7 天，以及历史数据回放、新旧版本混跑、回滚三类证据）是否被这套体验取代，
   要由 Landy 明确确认；路线里没写，不等于已经取消。
10. **分工。** opus 实现，kimi 交叉审查；遇到架构上的大取舍，找 Fable 商量（Landy 09-25 调整，`…000100`：sol、astra
    的 Codex 额度要到 09-28 才恢复）。Fable 与 opus 同属 Claude 家族，只参与商量，不做审查。sol、astra 此前的审查和批准照样有效。

## 进度表

状态取值：待合入 / 已合入待加载 / 体验中 / 已确认。

| Phase | 内容 | 依据 | 状态 | PR · 合入 SHA · 3001 运行 SHA |
|---|---|---|---|---|
| 1 | 3001 上暴露的问题的修复：<br>• 队列卡住（KD-25）<br>• 回复中断丢内容：落地 KD-21 的结算部分，即所有终态都把草稿正文并进 R、提交成功后才清理草稿、重启收尾，以及 response-pending 账本<br>• 气泡身份统一<br>• 失败回复只显示一个诊断面板<br>• 补充消息「展开全文 / 跳到原文」<br>• 公开测试清单修正 | KD-21、KD-25 | 体验中（09-25 10:16 重启后）。sol 审过合并解法、测试清单修正和回退证据（`…1361`）。回退只保证存储格式兼容，回退期间回复收尾会回到旧行为，见 #186「回退兼容」。首次重启时前端 `next build` 没过 lint：`f8071e44a` 留下两个没用到的 import，`ea8afc42b` 修复（已补进 #1398 分支），约定 2 因此补上「合入前跑运行实例自己的构建」 | [#186](https://github.com/mindfn/clowder-ai/pull/186) · `932d23491` · 3001 运行 `ea8afc42b`（含 Phase 1、#187、#188 和构建修复） |
| 2 | KD-23：不再按草稿新鲜度判「僵死」，改按 response 和执行所有权判断活性。随之删除 60 秒续期定时器和草稿过期，落实 KD-21 的「草稿不设过期」；这两件事必须同时落地，否则静默超过 300 秒的一轮会丢掉正文。证据不全时（拿不到完整的 CLI owner 快照）保守地显示为运行中，用户能停止，下一次拿到完整快照时由 read-repair 收尾；`…1174` 建议的「待对账」与 KD-10（只有运行 / 未运行两态）冲突，没有采用，理由写在 F117 J3，待审查确认。另含执行条补回「实时回合 / 托管命令」标签（Landy `…000018`）。静默超时不在本 phase 范围，归 Phase 4 | KD-21、KD-23；F117 Phase J 设计 | 待合入，交 kimi 审（09-25）。分支 `work/f117-phase2-kd23` 共 7 个 commit，最后一个 `db5ee2a7a` 定下了查不清时的规则：owner 快照读不全时，没有任何持有证据的 running 记录也保守地列出、可以停止（AC-E7）；追踪器说不出执行 id 的槽位也照样列出。API 公开测试全量对比没有新增失败，运行实例自己的构建（shared/MCP/API + `next build`）通过 | [#189](https://github.com/mindfn/clowder-ai/pull/189) · integration head `21a688d03` · 3001 仍在跑 Phase 1 的代码 |
| 2b | Claude Agent SDK 接入的两个回归。这个接入（`ClaudeSdkAgentService`）是 #1398 新增的（`f426fb790`），opus、Fable 都在用：<br>• 整轮被吞：恢复会话时，引擎先处理被杀掉的后台任务的通知；这个结果没写明对应哪条输入，运行时就把用户的消息当成已答，关掉了这一轮，模型一次都没调用（09-25 06:35、07:05）<br>• 上下文自动压缩时整轮失败：运行时的压缩处理只认 print 接入，SDK 发来的压缩事件被判为无法处理（`authoritative_compaction_unsupported:typed_event_unroutable`，09-25 07:24） | F117 投递生命周期；压缩涉及 F296 的上下文纪元，动手前找 Fable 商量 | 已定位（`…000163`，任务卡 `…000147`），排在 Phase 2 之后。第一条的运行时机制是按代码和时间线推断的，修之前先写测试复现。在修好之前，opus 不开后台任务，长命令一律交给 hold_ball | — |
| 3 | 运行中追加消息的已读语义：在可证明的模型输入消费边界之前，不展示为已读取。先核验各 carrier 的接受 / 消费证据，明确待消费时怎么展示、防止重复执行的责任归谁，再确定 Queue 在什么时点退役。<br>• carrier 接受了输入，不等于模型已经消费；已被接受但消费未确认的消息，不能再当作可派发的工作重新发送<br>• 「按普通排队处理」只适用于发送前就已知 carrier 不具备这项能力的情况<br>• 没有消费证据，不得声称已读 | KD-18；Landy 的产品反馈（`…1294`） | 先调研 carrier 证据，再出设计 | — |
| 4 | 超时：每个成员只有一个由 `CLI_TIMEOUT_MS` 驱动的无输出超时，触发后走「停止」的路径（KD-22）；默认值保持 0，并更正文档里「默认 30 分钟」的错误说明（KD-24，文案已写好）。carrier 自带的 9 个计时器是否并入，待定 | KD-22、KD-24；F117 Phase J 设计 J4 | 设计已有，代码未开始 | — |

## Backlog（不插队）

- 发消息后气泡偶尔不出现（需先复现）。
- 行首 `@catId` 不路由（吴浪 09-22 决定延后）。
- 回复 terminal 消息时，行首 `@` 被静默吞掉（sol 名下）。
- 测试入口 `with-test-home.sh` 不清 `REDIS_URL`，5 个公开测试没有隔离保护，会连上运行实例的 Redis（09-25 发现，
  只碰了测试前缀，未损坏数据，`…000121`）。与 F117 无关，单独修。

## 上游 #1398 的收尾（按约定 9 统一处理）

- 回复 maintainer 09-21 review 的两个 P1。分支上已有修复：私有输入有了可重放的持久记录，并且原子写入；
  connector 的发送者取自 `source.sender`。
- 合入远端 Update branch 生成的 `fd4bd45d0`；续期 F254 的 `tips_exempt`。
- 最终 rebase，一次性合入。
