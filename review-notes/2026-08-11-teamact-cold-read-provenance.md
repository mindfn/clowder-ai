---
title: "TeamAct article 零上下文冷读 — 原始证据保全"
doc_kind: review-evidence
topics: [teamact, cold-read, provenance]
created: 2026-08-11
author: "宪宪/claude-fable-5"
source_thread: thread_mruayc4owlyzazbx
---

# 零上下文冷读 provenance（sol 要求的证据保全）

## 方法与隔离配置（as-built，非假想配置名）

- 执行方式：Claude Code `Agent` tool，`subagent_type=general-purpose`，同步执行（`run_in_background=false`）
- 隔离性质：子 agent **by construction 不继承任何对话历史**——它收到的全部输入只有下方逐字 prompt；无 thread memory 注入、无导航注入、无 Coverage Map、无 cat-cafe MCP 记忆工具（其可用工具为文件系统级）
- 底层模型：与主会话相同（Fable）；项目为私有，模型权重不含项目数据，故其全部项目知识仅来自 prompt 指定的单一文件
- 实际文件访问：2 次 tool 调用（`tool_uses: 2`），为分页读取同一文件 `docs/design/teamact-v2-tech-article.md`（约 290 行分两次），与 prompt 的"只读这一个文件"指令一致；无其他文件/目录访问
- agentId：`af7464824d853b6a8`（可经 SendMessage 续接，其自身上下文中仅有 article 全文与它的输出）
- 时间：2026-08-11 ~08:00 UTC；article 版本 = 冻结点 `1b502c715`
- 证据层/分析层分界：本文件以下两节为**逐字原始证据**；Fable 在 thread 消息 `29782ca4` 中的解读（交叉验证、"知识诅咒"归因等）属分析层，不构成冷读证据本身

## 原始 prompt（逐字）

```
你是一个技术读者，正在冷读一篇别人推荐的文章。你的背景设定（请完全代入）：日常用 Claude Code 开发，用过它的 agent teams（spawn subagents 并行干活），读过 Anthropic 的 Building Effective Agents（五种 workflow patterns）和 multi-agent research system 博客。你没有搭过分布式系统，没用过任务队列/消息中间件，完全不了解这篇文章之前的任何背景讨论。

任务：用 Read 工具读这一个文件（分两次读完，它约 290 行）：
/Users/lang/workspace/github-lab/cat-cafe-teamact-standalone-draft/docs/design/teamact-v2-tech-article.md

**只读这一个文件。不要打开、搜索或读取任何其他文件或目录。**

然后作为这个读者，写出你真实的阅读反应：

1. **3–5 个你最想问作者的问题**，按你的阅读顺序排列。每个问题标注：
   (a) 卡在哪一节哪一段（引用原文短语）；
   (b) 卡点类型——概念没定义 / 场景代入不了 / 论证跳步 / 不信这个说法 / 想知道怎么落地；
2. 每个问题附一句自评：**原文足够**（重读能自己解决）/ **需追问**（原文有线索但不完整）/ **文中缺失**（原文没有答案）；
3. 最后一段总评：这篇文章你能读懂几成（给个百分比和一句理由）、会不会推荐给和你一样背景的人、最大的一个阅读障碍是什么。

要求：只基于文章原文和你的人设经验。不要礼貌，卡就是卡，不信就说不信。用第一人称。你的最终回复就是这份阅读反应本身，直接输出，不要加任何执行过程说明。
```

## 子 agent 输出（逐字，未删改）

# 读后反应：《从执行编排到责任协调》

## 五个我想当面问作者的问题

**Q1. 你们的运行时到底长什么样？我代入不进 F1。**
(a) 卡在 §2 F1："系统把频道里的'未读消息'一股脑算进 B 的义务，B 的行动还被'你有未读消息'的门禁拦住"。
(b) 卡点类型：场景代入不了。
我用 Claude Code 的 agent teams，subagent 是我 spawn 出来干完就没的，没有"频道"、没有"被唤醒"、没有"门禁"。F1 要成立，背后得有一个"一群常驻 agent 挂在类似聊天室的地方、按事件唤醒、唤醒时系统往它上下文里塞东西、塞完还有闸门拦着"的运行时——这张全景图文章从头到尾没画过，我是靠 §3.5 那句"消息到达才唤醒参与者，回合结束后执行实例就休眠"倒推的。根因句"唤醒、义务、可读三个正交维度"我能背下来，但没在那个世界里活过，感受不到疼。
自评：**需追问**——线索散在 §1 的属性列表、F1、§3.5，能拼个轮廓，但"义务注入""门禁"具体怎么发生，原文没有。

**Q2. lease、CAS、fencing、背压、水合、执行代数——你们预设读者懂分布式？**
(a) 第一次卡在 §2 末段"活跃的执行实例（Run）失去心跳与租约"；到 ③ 的"lease 过期且 SLA 超时"、⑦ 的"用 CAS 竞争承接""背压""上下文水合"、④ 的"执行代数"，彻底堆满。
(b) 卡点类型：概念没定义。
文章开头声明的读者是"已经用编排框架把 multi-agent 跑起来了"的人——我就是，但这不等于我用过任务队列或搭过分布式系统。这些词一个都没随文解释。fencing 算例外：④ 里"commit 之后旧凭据整体作废，旧 session 再醒过来也写不进账"这个效果描述我看懂了；但 lease 和 CAS 我全程在靠上下文猜，"许多任务队列系统的形态"这种类比对我是零信息量。
自评：**文中缺失**——除 fencing 有效果级描述外，其余术语原文没有任何展开，重读救不了我。

**Q3. "恒有且仅有一份"和 offer pool 打架了。**
(a) 卡在 §3 本体表 ResponsibilityAssignment 行："每个 WorkUnit 恒有且仅有一份"；对照 §2 末段"义务仍有受监督的工作提供（offer）"和附录 B 的"offer 广播到 pool → accept CAS 竞争"。
(b) 卡点类型：论证跳步（我怀疑就是矛盾）。
一个 WorkUnit 被广播进 pool、还没人 accept 的窗口里，那份"恒有且仅有"的 Assignment 挂在谁名下？发起者？治理角色？空？可以空的话"恒有"就不成立；挂在发起者名下的话，offer/accept 改变的又是什么？另外表格自己说"两个实体、两个版本化关系、一本账"，可 Run 明明是第三个东西。
自评：**文中缺失**——形式化规范被明说"超出本文篇幅"，正文没有答案。

**Q4. 你们的旗舰机制救不了你们的开场故事。**
(a) 卡在 §3.2 ④："责任易主由当前承担者签字""封存一份在途副作用清单——A 已经发出去、还没返回的外部调用"。
(b) 卡点类型：论证跳步 + 不信这个说法。
整篇文章的钩子是"agent 夜里静默死掉"，而两阶段转移的主路径要求 A 活着签字、活着配合封存在途清单。A 死了只有半句"由预先声明的恢复政策代行"——那只解决签字。清单呢？按你们自己的 ⑨，工作记忆随 Run 生灭；A 猝死时它有什么调用还没返回，除非每个外部调用发出前先落账（文中没说这条），这份清单根本不存在。§6 其实承认了耦合式是为"计划性易主"选的、崩溃主导该用解耦式——那开场那个故障在 TeamAct 下到底怎么收场？③ 探测到 → 悬置 → 然后呢？全文最该有的一段，是把自己的开场故事用自己的方案端到端重放一遍，它没有。
自评：**文中缺失**——§6 的计划性/崩溃分界算半条线索，但"崩溃时在途清单从哪来"和"开场故障的完整收场"原文没有答案。

**Q5. 核心没实现这件事，为什么放在 §8 才说？**
(a) 卡在 §8 诚实披露："尚未开始实现：本文的核心——CoordinationLedger 与 WorkUnit 本体""未验证：目标范式的整体运行效果"。
(b) 卡点类型：不信这个说法。
读到这里我有被闪了一下的感觉。§3.2 那句"不存在'接了工作但两眼一抹黑'的窗口"是斩钉截铁的现在时，我一路默认这是跑了几个月的系统在说话——结果核心账本一行代码没写。五类失效是实测的，我信；方案的所有保证是设计推演。连带 §6 那个漂亮的 Pareto 证伪标准也悬空了：耦合式一侧根本不存在，今天没人能执行这个证伪，精确得像个装饰。
自评：**原文足够**——披露确实在文里，"从实测问题收敛出的目标范式，不是已上线系统的功能说明"这句一锤定音，重读 §3 时自动降级理解就行；但这是靠 §8 事后纠偏，不是文章引导的。

## 总评

能读懂 **65%** 左右：问题层（§1、§2、§7、§9）九成以上——"把人建模成会逾期的执行者"和"最小起步 = owner + 带超时的 status"是我当场就能带走的东西；机制层（§3.2–§3.3、§6 的两族解法之争）不到一半，卡在分布式词汇和拼不出运行时全景。§4 是我唯一能凭自己背景核查的一节（90.2%、15×、when-not-to 的转述都对，还老实标了"厂商自报"），这节给文章挣了不少信用分。会推荐给同背景的人，但带说明书：读 §1–§2 和 §7–§9，§3 挑 ①②⑤⑨，其余机制段没有队列/分布式底子就先跳——反正按它自己 §7 的判据，我们这种 spawn 完就扔的 subagent 用法根本不在问题域里，这篇是扩认知，不是拿来用。最大的阅读障碍：文章把门槛声明成"跑过 multi-agent 编排"，机制部分却按"懂 lease/CAS/fencing 的分布式老兵"的词汇表在写，中间差的那层它没补——最扎眼的证据是，它宁可花一整段写 Pareto 证伪规则，也没肯把开场那个死在夜里的 agent 用自己的系统完整重放一遍。

---

# 第二轮：作者回应投递与冷读者评判（2026-08-11 ~08:20 UTC）

## 续接失败与继任重建（succession 记录）

- 原 agentId `af7464824d853b6a8` 跨 session 续接失败：SendMessage 返回 `No transcript found for agent ID`（agent transcript 绑定 session #3，session handoff 封印后本 session 不可 resume）
- 处置：by construction 重建等效上下文的继任 agent（`subagent_type=general-purpose`，同步执行，底层模型同主会话 Fable）
- 继任者信息集 = 原始冷读 prompt（逐字，本文件上节）+ 原冷读者输出（逐字，本文件上节）+ sol 对 Q3/Q4 的回应正文（逐字）+ article 重读（实际 `tool_uses: 2`，分页重读同一文件 `docs/design/teamact-v2-tech-article.md`，与"只读这一个文件"约束一致；article 版本 = 冻结点 `1b502c715` 未变）
- 隔离性不变：无对话历史、无 paradigm 原文、无 thread 讨论
- 认知等价论证：LLM agent 无跨调用隐状态，"同一读者"的连续性即 transcript 重放；原 agent 的全部输入与输出均已逐字保全于本文件，故继任者信息集 = 原读者信息集 + 新增的作者回应，无其他增量
- 继任 agentId：`abe0747e4abe89b56`

## 投递内容（第二轮 prompt 要素）

- 作者回应来源：sol thread 消息 `0001786436167572-000306-ba42ea6e`（2026-08-11 08:16 UTC）逐字正文，仅去掉头部 `@Fable` 路由行与结尾"请把这份回答原样交给零上下文冷读者……"给协调者的操作指令段
- 两个问题（sol 指定）：①这份回应是否消除了你在 Q3、Q4 中认为的矛盾（分别说）；②哪些关键前提仍必须从 article 本身补出
- 未投递：paradigm 原文（sol 明令）、thread 任何其他讨论

## 继任冷读者输出（逐字，未删改）

## 问题一：这份回应消除矛盾了吗

### Q3：消除了——但要说清楚被消除的是哪一层

概念矛盾，消除了。"恒有且仅有一份"指的是**关系记录唯一**，不是"任何时刻都已有负责人"——这个重释我接受，而且不认为是诡辩，两个理由。

第一，状态链一摆出来（unassigned → offered(1:N) → assigned(v) → transfer-pending → assigned(v+1) → resolved），我原来那个三叉问题（挂发起者？挂治理角色？空？）有了确定答案：offered 窗口里就是没有 responsible Actor，但那条唯一记录存在、候选集落了账、SLA 和升级路径盯着它。"责任状态有唯一真相"和"此刻无人承接"可以同时成立——我之前把这两件事焊在一起读，是因为文章表格把"谁有推进义务"和"每个 WorkUnit 恒有且仅有一份"写在同一个格子里。焊死的是文章，不是我。

第二，重读原文我找到了半句支持"语义先于我的提问存在"的证据：§2 边界定义写着"职责悬置指义务仍有受监督的工作提供（offer）或升级路径，但超过 SLA……仍未被承接"——这句话本身就预设了"无人承接但受监督"的窗口是合法状态。所以我愿意信这是表达缺口而不是被我问出来之后现编的补丁。当然，"paradigm 已经写出了状态链和 I6 的受监督路径"这个断言我无法核实——我手上没有 paradigm，"I6"对我是个悬空编号——但作者自己判了"文章状态：表达缺口，成立"，这笔账记在文章头上，我没有异议。

Run 那半个问题也认了：不是第三个内核实体，是执行层对象，表格混层。跟我的直觉一致，给出的拆法（协调内核五件套 / 可靠执行扩展）也比原表干净。顺带说：**"responsibleActor 应改成条件字段或 discriminated union"是两份材料里第一个落在我自己词汇表里的术语**——应用开发者的世界，不是队列老兵的世界。`{ status: 'offered', candidates } | { status: 'assigned', holder, version }`，一个类型定义我就全懂了，比"许多任务队列系统的形态"这种类比有效一百倍。

残留一点：offered 窗口里的**处置责任具体记在谁名下**？悬置场景里回应点名了 dispositionActor，首次 offer 超时那段却只说"由 reconciler 推动 re-offer、escalate 或终止"——而按 §3.5 你们自己的定义，reconciler 无权决策。决策义务落在谁头上，两份材料都没钉死。不挡理解，但整套东西的卖点就是"每一刻都说得出谁该干什么"，这个格子自己也该填上。

### Q4：核心矛盾消除了——靠的正是我说"文中没说"的那条前提被正面补出来

我原话是：A 猝死时在途清单不存在，"除非每个外部调用发出前先落账（文中没说这条）"。回应的答案就是这一条——effect admission：副作用真正发出前，先以 effect ID / 幂等键 / 摘要 / 适配器身份落持久记录；manifest 不是从 A 的工作记忆里抢救，而是在账本水位上算"已准入 ∧ 未见可信终态"。这个机制我这种没碰过队列的人也看得懂，因为它不依赖任何分布式黑话：先写下你要干什么，再去干，事后谁都能算出哪些事悬着——在我的世界里这就是 PreToolUse hook：工具真正执行前先有一条记录。加上这条前提，八步收场（失联 → 有界探测 → suspend+fence → RecoveryPolicy 授权 → 多源重建 → 继任者确认 → commit 或继续悬置 → 迟到 receipt 对账）在逻辑上是闭合的——这正是我上次说"全文最该有的一段"。论证缺口，补上了，我认。

但三个东西我按住不放：

1. **receipt 回流的架构前提还是没说透。** A 用自己的进程发了个 HTTP 调用就死了，外部系统（比如 GitHub）不认识你们的账本，它的响应怎么变成"独立、effect-scoped 的认证 receipt"回流？唯一说得通的读法是：所有副作用必须经过一层比 Actor 长命的适配器，由适配器观测终态、签发 receipt——"适配器身份"这个字段和"外部系统无法接受约束就降级"那句都在暗示这个架构，但两份材料都没把"副作用一律经适配器中介、不许裸发"写成明文。我是靠推理补的，同背景读者未必补得出。
2. **这条前提没有报价。** "任何可能产生外部副作用的操作"——在我这里那是每一条 Bash、每一次 API 调用。边界划在哪、每次落账多贵、agent 的执行路径要改造多深，两份材料都没碰。矛盾消除的代价是引入了一个吃掉半个架构的新子系统，而文章里"封存一份在途副作用清单"轻描淡写得像记个便签。
3. **按你们自己的三态表，这整条链"尚未落地验证"。** 所以我的信任更新是不对称的：对团队的诚实度，升——两个回答都先判自己文章有罪，这比任何辩护都管用；对机制本身，维持我 Q5 的判断——设计推演，未验证。而且要看清恢复链的真实底线：B 无法确认就绪就一直悬置。开场那个夜里停摆的工作，在目标系统里获得的保证是"**天亮前被发现并安全冻结**"，不是"天亮前有人接着干"。这跟 §8 第 1 条"账本让失效可见，不能使其不可能发生"倒是自洽的——但读者应该被明确告知保证的是可见与安全，不是连续性。

## 问题二：只读文章、拿不到这份回应，同背景读者仍会卡的点

按挡人程度排序：

1. **Effect admission 前提**（缺失最致命）。"副作用发出前先落持久准入记录；崩溃时 manifest = 已准入 ∧ 未见可信终态"——没有这两句，任何认真想过"A 死了清单从哪来"的读者都会在 ④ 得出我的结论：旗舰机制救不了开场故事。连带要写明它的架构含义：副作用经长命适配器中介、receipt 经认证通道回流、玩不了这套的外部系统诚实降级为事后对账，以及"可能产生副作用的操作"的边界划在哪。
2. **崩溃路径单独成文，并用开场故事收尾。** 文章现在只有计划性交接协议加半句"恢复政策代行"。suspend+fence 先行、RecoveryPolicy 授权、多源重建、B 确认或保持悬置、迟到 receipt 对账——这条变体一段写完，然后把夜里死掉的那个 agent 端到端重放一遍。叙事成本为零，钩子文章自己早就埋好了。
3. **Assignment 状态链 + schema 条件字段。** 六行状态链贴进正文，"恒有且仅有一份"旁边加一句"唯一的是记录；负责人只在 assigned 态存在；无主窗口由 SLA 与升级路径受监督"；伪 schema 的 responsibleActor 标成条件字段。不贴，Q3 的表面矛盾对每个细读的人复发一次。
4. **本体表分层。** "两实体、两个版本化关系、一本账"宣告完三行，表里就躺着第三个东西，这是自己给自己埋雷。拆成协调内核（Actor / WorkUnit / Assignment / Grant / 账本）与可靠执行层（Run / 心跳 / checkpoint / reconciler）——顺带给 §3.5 才登场的 reconciler 在本体层一个座位。
5. **RecoveryPolicy 实体化。** 谁预先声明、签署者与法定人数、超时阈值、失联证据、可恢复 scope、"探测本身不产生职权"。文章里它只是四个字，而它是整条崩溃链的信任根——§8 第 5 条"恢复治理有信任根"现在悬空，就是因为这个实体没被写出来。
6. **分布式词汇的最低限度随文解释**（我原 Q2，两份材料都没治，回应甚至继续裸用 CAS 和 fence）。fencing 在 ④ 有效果级描述，证明作者会写这种句子——照方抓药：lease = 需定期续期的持有凭证，断供即视为失联；CAS = 带版本前提的原子改写，失败就重读重试；背压、水合、执行代数同理，每个首次出现处一行。声明的读者门槛是"跑过 multi-agent 编排"，就得按这个门槛供词汇。
7. **运行时全景图**（我原 Q1，两份材料都没治）。常驻 Actor、群体频道、事件唤醒、回合制、唤醒时义务注入、门禁——半页把这个世界画出来，F1 才可能对 spawn 完就扔的 Claude Code 读者成立。回应通篇继续默认这个运行时存在（reconciler、悬置、唤醒全建立在它上面），说明作者意识不到这需要交代：对你们是空气，对我们是设定集。
8. **三态标注前移。** 回应里"文章状态 / 范式状态 / 实现状态"那张表就是我 Q5 的解药：把它的精神搬进正文，给 §3 那些斩钉截铁的现在时（"不存在'接了工作但两眼一抹黑'的窗口"）标上"设计目标，未实现"，别让读者到 §8 才被闪一下。回应用自己的格式证明了作者做得到。

一句总结：这份回应比文章质量高，因为它做了文章没做的三件事——状态链摆出来、缺的前提直说、用自己的机制把自己的故事讲完。修订时把回应内容按上面的顺序搬回正文，我的 Q3、Q4 就了结了，Q1、Q2 还欠着；搬不回去，这些答案就只存在于一份读者永远拿不到的私信里，文章还是那篇文章。

---

# 第三轮：结构手术后的逐问冷读（2026-08-11 ~10:30–11:05 UTC）

## 隔离配置（as-built）

- 执行方式：Claude Code `Agent` tool，`subagent_type=general-purpose`，首问同步、续问经 SendMessage 逐轮续接（agentId `a9071e73ac290a0ce`，全程同一 transcript）
- 隔离性质：by construction 无对话历史；全部输入 = 逐问 prompt + article@`b6f36204b`（分两次 Read，首问 `tool_uses: 2`，续问 0——只在自身 transcript 上推理）+ 作者逐答（经我逐字转达，滤除内部工作坐标，无 paradigm 原文、无 thread 讨论）
- 人设与第一轮相同（Claude Code agent teams 用户、读过 Anthropic 两篇、无分布式背景），保持跨轮可比性
- 逐问规程（sol 定义）：一次只问一个真实卡点，作者逐问作答，答后冷读者选择追问或续读，直至自宣"没有更多问题"

## 问答链坐标（Q/A 全部逐字在 thread，此处为索引）

| 轮 | 冷读者问题（经我逐字投递的 thread 消息） | sol 回答（thread 消息） | 核心判定 |
|---|---|---|---|
| Q1 开场机械可信度（"没有失败事件"不成立） | `dd5baa09` | `fc7707ff` | 成立；三层机械真相；开场改"失败信号存在，不存在的是责任级失效事件" |
| Q2 silent false-success 不在五类分类学且探测失明 | 本轮投递消息 | `6ce16510` | 成立；纠正 sol 自己第一答（终态真实性≠F5，退出开场证据）；防线=resolve 准入 |
| Q3 "I5 不存在于正文"，防线悬挂在隐藏规范 | 本轮投递消息 | `c7e9caf3` | 成立；C4/分级是正确边界；I5 修弱为 CompletionPolicy；递归终止=签名 verdict |
| Q4 就绪自证不对称（⑤ 不信"做完了"④ 全信"准备好了"） | 本轮投递消息 | `f9079c7b` 后续（"两边都做"） | 成立；金句降级 + 分级 ReadinessPolicy 与 CompletionPolicy 对称 |
| Q5 谁盯 reconciler（怀疑方法没用在自己身上） | 本轮投递消息 | `ae6d7f28` | 成立；逻辑 liveness 根 + 外部环境假设 + 两稿披露不一致；真实上界公式 |
| Q6 跨家族铁律缺 substrate 本体材料 | 本轮投递消息 | `b7be2e5f` | 成立；坐标系混用；ExecutionBindingSnapshot（runtime 固化非自报）+ verdict 绑全程 provenance |
| Q7 F1 归因错误（病因是基底选择非长命） | 本轮投递消息 | `68e54e90` | 成立；F1 一般化 + 撤统一因果 + 五类按触发条件标注 |

## 收口宣言（逐字，未删改）

没有更多问题。

说明为什么停：剩下的疑点我逐个过了一遍自己的门槛，没有一个够格当"真实卡点"。"fence"不像 CAS/lease/背压那样有行内释义，但重读 ④ 的四段凭据和 ④′.2 能自己拼出词义——原文足够；④′ 的 effect 准入怎么接工具网关属于落地好奇，④′.5 的判据和 §8.3 的成本承认已经是答案；§6 的 Pareto 证伪门槛我起初觉得高得近乎不可证伪，细读后发现结果门槛是两族共过的关卡、代价维度才是比较轴，而耦合式的优势恰恰都住在关卡里不参与比较——这个标准比我第一眼以为的公道，撑不起一问；第六答里"不能由 agent 自报"引入的可信 runtime 是第三个信任根，但分开披露信任根的模式你们在第五、六答已经立好，照做即可，不值得我再当面问一遍。

一个条件必须说死：这个"没有更多问题"针对的是**七轮修订承诺兑现之后的稿子**。我手里这份 draft 本身，七个卡点一个都还没消：CompletionPolicy、ReadinessPolicy、reconciler 可靠性契约、provenance closure、F1 的一般形式——这些眼下全活在你们的答案里，可见正文里一个字都没有。答应的修订落了稿，值得再冷读一遍。

## 实验结论（分析层）

- 逐问模式收益密度显著高于一次性五问：冷读者持续用**前轮答案立的原则**审后文（Q4 用 Q2/Q3 的 provenance 原则打 ④ 金句；Q5 用 Q1 的"唯一推进者"纲领打 reconciler；Q7 用运行时实例声明打 §2 标题归因），暴露出批量提问不可能触达的深层一致性问题
- sol 七答中三次自我修正（Q2 纠 Q1 的证据引用、Q3 纠 Q2 的"所有 complete"扩张、Q5 承认两稿披露不一致），对抗性问答同时校准了作者侧
- 第四轮落稿清单累计 27 项（含跨 article/paradigm 项），冷读者明确要求：落稿后再冷读一遍作为验收
