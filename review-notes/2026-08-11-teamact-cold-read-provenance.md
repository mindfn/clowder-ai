---
title: "TeamAct article 零上下文冷读 — 原始证据保全"
doc_kind: review-evidence
topics: [teamact, cold-read, provenance]
created: 2026-08-11
author: "宪宪/claude-fable-5"
source_thread: thread_mruayc4owlyzazbx
---

# 零上下文冷读 provenance（sol 要求的证据保全）

## 方法与隔离配置（as-built，非假想配置名）

- 执行方式：Claude Code `Agent` tool，`subagent_type=general-purpose`，同步执行（`run_in_background=false`）
- 隔离性质：子 agent **by construction 不继承任何对话历史**——它收到的全部输入只有下方逐字 prompt；无 thread memory 注入、无导航注入、无 Coverage Map、无 cat-cafe MCP 记忆工具（其可用工具为文件系统级）
- 底层模型：与主会话相同（Fable）；项目为私有，模型权重不含项目数据，故其全部项目知识仅来自 prompt 指定的单一文件
- 实际文件访问：2 次 tool 调用（`tool_uses: 2`），为分页读取同一文件 `docs/design/teamact-v2-tech-article.md`（约 290 行分两次），与 prompt 的"只读这一个文件"指令一致；无其他文件/目录访问
- agentId：`af7464824d853b6a8`（可经 SendMessage 续接，其自身上下文中仅有 article 全文与它的输出）
- 时间：2026-08-11 ~08:00 UTC；article 版本 = 冻结点 `1b502c715`
- 证据层/分析层分界：本文件以下两节为**逐字原始证据**；Fable 在 thread 消息 `29782ca4` 中的解读（交叉验证、"知识诅咒"归因等）属分析层，不构成冷读证据本身

## 原始 prompt（逐字）

```
你是一个技术读者，正在冷读一篇别人推荐的文章。你的背景设定（请完全代入）：日常用 Claude Code 开发，用过它的 agent teams（spawn subagents 并行干活），读过 Anthropic 的 Building Effective Agents（五种 workflow patterns）和 multi-agent research system 博客。你没有搭过分布式系统，没用过任务队列/消息中间件，完全不了解这篇文章之前的任何背景讨论。

任务：用 Read 工具读这一个文件（分两次读完，它约 290 行）：
/Users/lang/workspace/github-lab/cat-cafe-teamact-standalone-draft/docs/design/teamact-v2-tech-article.md

**只读这一个文件。不要打开、搜索或读取任何其他文件或目录。**

然后作为这个读者，写出你真实的阅读反应：

1. **3–5 个你最想问作者的问题**，按你的阅读顺序排列。每个问题标注：
   (a) 卡在哪一节哪一段（引用原文短语）；
   (b) 卡点类型——概念没定义 / 场景代入不了 / 论证跳步 / 不信这个说法 / 想知道怎么落地；
2. 每个问题附一句自评：**原文足够**（重读能自己解决）/ **需追问**（原文有线索但不完整）/ **文中缺失**（原文没有答案）；
3. 最后一段总评：这篇文章你能读懂几成（给个百分比和一句理由）、会不会推荐给和你一样背景的人、最大的一个阅读障碍是什么。

要求：只基于文章原文和你的人设经验。不要礼貌，卡就是卡，不信就说不信。用第一人称。你的最终回复就是这份阅读反应本身，直接输出，不要加任何执行过程说明。
```

## 子 agent 输出（逐字，未删改）

# 读后反应：《从执行编排到责任协调》

## 五个我想当面问作者的问题

**Q1. 你们的运行时到底长什么样？我代入不进 F1。**
(a) 卡在 §2 F1："系统把频道里的'未读消息'一股脑算进 B 的义务，B 的行动还被'你有未读消息'的门禁拦住"。
(b) 卡点类型：场景代入不了。
我用 Claude Code 的 agent teams，subagent 是我 spawn 出来干完就没的，没有"频道"、没有"被唤醒"、没有"门禁"。F1 要成立，背后得有一个"一群常驻 agent 挂在类似聊天室的地方、按事件唤醒、唤醒时系统往它上下文里塞东西、塞完还有闸门拦着"的运行时——这张全景图文章从头到尾没画过，我是靠 §3.5 那句"消息到达才唤醒参与者，回合结束后执行实例就休眠"倒推的。根因句"唤醒、义务、可读三个正交维度"我能背下来，但没在那个世界里活过，感受不到疼。
自评：**需追问**——线索散在 §1 的属性列表、F1、§3.5，能拼个轮廓，但"义务注入""门禁"具体怎么发生，原文没有。

**Q2. lease、CAS、fencing、背压、水合、执行代数——你们预设读者懂分布式？**
(a) 第一次卡在 §2 末段"活跃的执行实例（Run）失去心跳与租约"；到 ③ 的"lease 过期且 SLA 超时"、⑦ 的"用 CAS 竞争承接""背压""上下文水合"、④ 的"执行代数"，彻底堆满。
(b) 卡点类型：概念没定义。
文章开头声明的读者是"已经用编排框架把 multi-agent 跑起来了"的人——我就是，但这不等于我用过任务队列或搭过分布式系统。这些词一个都没随文解释。fencing 算例外：④ 里"commit 之后旧凭据整体作废，旧 session 再醒过来也写不进账"这个效果描述我看懂了；但 lease 和 CAS 我全程在靠上下文猜，"许多任务队列系统的形态"这种类比对我是零信息量。
自评：**文中缺失**——除 fencing 有效果级描述外，其余术语原文没有任何展开，重读救不了我。

**Q3. "恒有且仅有一份"和 offer pool 打架了。**
(a) 卡在 §3 本体表 ResponsibilityAssignment 行："每个 WorkUnit 恒有且仅有一份"；对照 §2 末段"义务仍有受监督的工作提供（offer）"和附录 B 的"offer 广播到 pool → accept CAS 竞争"。
(b) 卡点类型：论证跳步（我怀疑就是矛盾）。
一个 WorkUnit 被广播进 pool、还没人 accept 的窗口里，那份"恒有且仅有"的 Assignment 挂在谁名下？发起者？治理角色？空？可以空的话"恒有"就不成立；挂在发起者名下的话，offer/accept 改变的又是什么？另外表格自己说"两个实体、两个版本化关系、一本账"，可 Run 明明是第三个东西。
自评：**文中缺失**——形式化规范被明说"超出本文篇幅"，正文没有答案。

**Q4. 你们的旗舰机制救不了你们的开场故事。**
(a) 卡在 §3.2 ④："责任易主由当前承担者签字""封存一份在途副作用清单——A 已经发出去、还没返回的外部调用"。
(b) 卡点类型：论证跳步 + 不信这个说法。
整篇文章的钩子是"agent 夜里静默死掉"，而两阶段转移的主路径要求 A 活着签字、活着配合封存在途清单。A 死了只有半句"由预先声明的恢复政策代行"——那只解决签字。清单呢？按你们自己的 ⑨，工作记忆随 Run 生灭；A 猝死时它有什么调用还没返回，除非每个外部调用发出前先落账（文中没说这条），这份清单根本不存在。§6 其实承认了耦合式是为"计划性易主"选的、崩溃主导该用解耦式——那开场那个故障在 TeamAct 下到底怎么收场？③ 探测到 → 悬置 → 然后呢？全文最该有的一段，是把自己的开场故事用自己的方案端到端重放一遍，它没有。
自评：**文中缺失**——§6 的计划性/崩溃分界算半条线索，但"崩溃时在途清单从哪来"和"开场故障的完整收场"原文没有答案。

**Q5. 核心没实现这件事，为什么放在 §8 才说？**
(a) 卡在 §8 诚实披露："尚未开始实现：本文的核心——CoordinationLedger 与 WorkUnit 本体""未验证：目标范式的整体运行效果"。
(b) 卡点类型：不信这个说法。
读到这里我有被闪了一下的感觉。§3.2 那句"不存在'接了工作但两眼一抹黑'的窗口"是斩钉截铁的现在时，我一路默认这是跑了几个月的系统在说话——结果核心账本一行代码没写。五类失效是实测的，我信；方案的所有保证是设计推演。连带 §6 那个漂亮的 Pareto 证伪标准也悬空了：耦合式一侧根本不存在，今天没人能执行这个证伪，精确得像个装饰。
自评：**原文足够**——披露确实在文里，"从实测问题收敛出的目标范式，不是已上线系统的功能说明"这句一锤定音，重读 §3 时自动降级理解就行；但这是靠 §8 事后纠偏，不是文章引导的。

## 总评

能读懂 **65%** 左右：问题层（§1、§2、§7、§9）九成以上——"把人建模成会逾期的执行者"和"最小起步 = owner + 带超时的 status"是我当场就能带走的东西；机制层（§3.2–§3.3、§6 的两族解法之争）不到一半，卡在分布式词汇和拼不出运行时全景。§4 是我唯一能凭自己背景核查的一节（90.2%、15×、when-not-to 的转述都对，还老实标了"厂商自报"），这节给文章挣了不少信用分。会推荐给同背景的人，但带说明书：读 §1–§2 和 §7–§9，§3 挑 ①②⑤⑨，其余机制段没有队列/分布式底子就先跳——反正按它自己 §7 的判据，我们这种 spawn 完就扔的 subagent 用法根本不在问题域里，这篇是扩认知，不是拿来用。最大的阅读障碍：文章把门槛声明成"跑过 multi-agent 编排"，机制部分却按"懂 lease/CAS/fencing 的分布式老兵"的词汇表在写，中间差的那层它没补——最扎眼的证据是，它宁可花一整段写 Pareto 证伪规则，也没肯把开场那个死在夜里的 agent 用自己的系统完整重放一遍。
