# Test Skill

A minimal skill for F197 plugin framework verification.

## Usage

This skill does nothing useful. It exists to verify that the plugin framework can:
1. Discover a plugin with a skill resource
2. Enable the plugin and mount the skill via symlink
3. Disable the plugin and remove the symlink
4. Show the skill as grayed out when plugin is disabled
